// Momo configuration placeholder. Replace `merchantPhone` with your merchant account.
const momoConfig = {
    merchantPhone: "0399026084",
    // If Momo requires a specific partner/merchant id, add it here (e.g. partnerId: 'YOUR_PARTNER_ID')
    partnerId: "",
    // Custom app scheme used for return URL
    appScheme: "burgerappreactnative",
};

export default momoConfig;
